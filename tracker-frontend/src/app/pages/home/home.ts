import { Component } from '@angular/core';
import { Graph } from '../../components/graph/graph';

@Component({
  selector: 'app-home',
  imports: [Graph],
  templateUrl: './home.html',
  styleUrl: './home.css',
})
export class Home {

}
